Creating A main Folder in explorer
cretaing Nextjs app `npx create-next-app@latest` then give file name
then give yes/ no
Customize settings     → No
TypeScript             → No
Linter                 → ESLint
React Compiler         → No
import alias           → No
AGENTS.md              → No
create Backend Folder manually
enter in backend Folder run `npm install` to install package.json  
then install express, cors, bcrypt, mongoose, dotenv
change "type": "module",
to restart server node --watch server.js
creating folder controller , routes, models
writting route("/).get(activeCheck)
writting controller
writting model -> schema
